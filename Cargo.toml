[workspace]
members = [
    "hte",
    "core-vsa",
    "fabricator",
    "cli",
    "cognition",
    "memory",
    "engine",
    "ingestion",
    "learning",
    "api",
    "perception",
    "neural",
    "dataset",
    "gpu-bridge",
    "runtime",
    "multimodal",
    "xtask",
]
resolver = "2"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = 'abort'
strip = true
